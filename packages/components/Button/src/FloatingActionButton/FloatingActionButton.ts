import { Button } from '../Button';
import { settings } from './FloatingActionButton.settings';

export const FloatingActionButton = Button.compose({ displayName: 'FloatingActionButton', settings });
